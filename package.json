{
  "name": "zyayoung.github.io",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && gh-pages -d dist --nojekyll --message 'Deploy $(date +%Y-%m-%d_%H:%M:%S)'",
    "build:gh": "npm run build && cp ./CNAME ./dist/CNAME || true"
  },
  "dependencies": {
    "astro": "^5.17.1"
  },
  "devDependencies": {
    "gh-pages": "^6.1.0"
  }
}